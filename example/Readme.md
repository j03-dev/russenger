# Example

## how to run it change info inside `.env` file
## And run

### Create migration
```bash
cargo run migrate
```

### run the app
```bash
cargo run runserver
```

### You can customise the `Rocket.toml`
